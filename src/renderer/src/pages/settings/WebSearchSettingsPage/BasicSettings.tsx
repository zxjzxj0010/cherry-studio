import { FC } from 'react'

import { SettingContainer } from '..'

const BasicSettings: FC = () => {
  return <SettingContainer>This is setting container</SettingContainer>
}
export default BasicSettings
